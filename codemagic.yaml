workflows:
  android-workflow:
    name: BlackDeath Android
    max_build_duration: 60
    environment:
      flutter: 3.27.0
    scripts:
      - name: Get packages
        script: flutter pub get
      - name: Build APK
        script: flutter build apk --release
    artifacts:
      - build/**/outputs/**/*.apk
